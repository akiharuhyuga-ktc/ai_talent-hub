{
  "name": "ktc-talent-hub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm run install:web-demo",
    "dev": "npm --prefix web-demo run dev",
    "build": "npm --prefix web-demo run build",
    "start": "npm --prefix web-demo run start",
    "lint": "npm --prefix web-demo run lint",
    "install:web-demo": "npm --prefix web-demo install"
  }
}
